package com.ab.collection.concurrent;

/**
 * @author Arpit Bhardwaj
 *
 * Both are concurrent sorted collections
 *
 * ConcurrentSkipListSet
 *      implements NavigableSet<E>, interfaces.
 *      does not allow null elements.
 *      All the elements are sorted based on natural ordering or by the Comparator passed during it’s construction time.
 *
 * ConcurrentSkipListMap
 *      implements ConcurrentNavigableMap interface.
 *      does not allow null elements.
 *      All the elements are sorted based on natural ordering or by the Comparator passed during it’s construction time.
 */
public class ConcurrentSkipListSetAndMapDemo {
    public static void main(String[] args) {

    }
}
